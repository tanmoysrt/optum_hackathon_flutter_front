import 'package:flutter/material.dart';
import '../components/dynamic_icons.dart';

 class InsightsPage extends StatelessWidget {


   @override
   Widget build(BuildContext context) {
     return Container(
       padding: const EdgeInsets.all(20),
       child : Center(
         // child: _footstepsTargetCard(context, 250 , 85),
         // child: DynamicIcon(assetImage: "assets/images/footsteps.png", fillPercentage: 0.30, fillColor: Colors.amber, backgroundColor: Colors.transparent, height: 100, width: 100)
       )
     );
   }
}


